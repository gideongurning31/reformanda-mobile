import React from 'react';
import { Text, StyleSheet } from 'react-native';

const WartaScreen = () => <Text style={styles.text}>WARTA</Text>;

const styles = StyleSheet.create({
  text: {
    fontSize: 20,
    textAlign: 'center',
    textAlignVertical: 'center',
  },
});

export default WartaScreen;
